const pixelSize = 25;
const canvasWidth = pixelSize * 7 * 2;
const canvasHeight = pixelSize * 9 * 1;
const basicGreen = '#095709'; //healthy icon color
const frameRate = 10; // update frames every 10 milliseconds
